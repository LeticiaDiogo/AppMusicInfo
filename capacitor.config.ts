import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AppMusicInfo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
